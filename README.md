To run the code on your local machine
Download the git repository on your local, cd into `blogpostDjango\blogProject` in the terminal and run
`python manage.py runserver`
Open the url and append `signup` in it, something like this `http://127.0.0.1:8000/sign_up/`

